import { SafeAreaView } from "react-native";
import { Text } from "react-native";
import { InExTr } from "@/components";

export default function Index() {
  return (
    <SafeAreaView>
      <InExTr />
    </SafeAreaView>
  );
}
